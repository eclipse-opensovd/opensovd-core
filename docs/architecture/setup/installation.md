# Installation

To build and run the OpenSOVD Core, ensure you have a Rust compiler and SDK installed. We recommend using `rustup`.

Minimum required Rust version: 1.88.0

Build:
./build.sh start

Clean:
./build.sh clean
